{
  "name": "appo-alchemize--3-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "expo"
  ],
  "scripts": {
    "start": "cd expo && bun run start",
    "build:ios": "cd expo && eas build -p ios --profile preview",
    "build:android": "cd expo && eas build -p android --profile preview",
    "build:prod": "cd expo && eas build --profile production"
  }
}
